#!/usr/bin/env bash

D=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

if [ "$1" == "--build" ]; then
    shift
    pushd $D
    rebar3 as metrics escriptize || exit 1
    popd
fi

$D/_build/metrics/bin/etylizer "$@"
